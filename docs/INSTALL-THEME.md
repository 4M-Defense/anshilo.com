# התקנת הת'ים ופרסומו

מדריך לבעל החנות. אין צורך בידע בתכנות.

---

## 1. העיצוב החדש חי

העיצוב החדש הוא כבר הת'ים **המפורסם** של החנות. שמו בניהול:

> **`shilov8theme`**

**לצפייה:** https://anshilo.com — פשוט האתר.

> קודם נכתב כאן שהעיצוב לא מפורסם, עם קישור תצוגה מקדימה לת'ים
> `148368425039`. שני הדברים כבר לא נכונים: הת'ים פורסם, והת'ים ההוא הוא אחד
> מאותם ת'ימים מיושנים שרשומים למחיקה — כלומר צבעים ותמונות שהיו נערכים בו לא
> היו מופיעים באתר בכלל. אם ערכתם משהו לפי הגרסה הקודמת של המדריך הזה, שווה
> לבדוק שהשינוי אצלכם בת'ים המפורסם.

`שמירה 1` נשמרה כגיבוי לא מפורסם, וכך גם הגרסאות v5–v7 של העיצוב החדש. אפשר
למחוק את הגרסאות המיושנות: שלוש הנקודות שליד הת'ים → **מחיקה**. את `שמירה 1`
כדאי להשאיר.

---

## 2. מה כדאי לעבור עליו

העמודים האלה הם הרשימה שכדאי לבדוק אחרי כל שינוי, **גם במחשב וגם בטלפון**:

| עמוד | מה לבדוק |
|---|---|
| דף הבית | 12 המחלקות, המבצעים, הנמכרים, המותגים |
| קטגוריה — למשל `כלי עבודה` | הסינון בצד, המיון, מספר המוצרים |
| מוצר עם וריאציות — למשל נעל בלנסטון | בחירת מידה, המלאי, המחיר |
| מוצר ללא מחיר | צריך להופיע "מחיר בטלפון" ולא ₪0.00 |
| מוצר שאזל | צריך להופיע "עדכנו אותי כשחוזר למלאי" |
| חיפוש | הקלידו "מקיטה" — התוצאות צריכות להופיע תוך כדי הקלדה |
| `/pages/quick-order` | הדביקו כמה מקטים ובדקו שהם נמצאים |
| עגלה | הוסיפו מוצר ובדקו את פס המשלוח החינם |
| תפריט מגה | לחצו על "כלי עבודה" בשורת הניווט במחשב |

---

## 3. הפרסום כבר נעשה — ואיך חוזרים אחורה

הת'ים פורסם, ולכן אין כאן מה לעשות. הסעיף נשאר בשביל שני דברים:

**חזרה אחורה.** אם משהו נשבר: **חנות מקוונת → ערכות נושא** → שלוש הנקודות שליד
`שמירה 1` → **פרסום**. מיידי והפיך לגמרי.

**אפליקציות.** ת'ים חדש הוא ת'ים אחר, ולכן **אפליקציות שהיו מוטמעות בת'ים הישן
לא עברו אליו אוטומטית**. אפליקציית הנגישות (sense-rtl) חוברה מראש ואז כובתה,
כי הווידג'ט של הנגישות בנוי עכשיו בתוך הת'ים עצמו. אם יש אפליקציות נוספות
שחשובות לכם (תגיות מוצר, ניוזלטר, ביקורות) — צריך להיכנס לכל אחת ולהפעיל אותה
מחדש על הת'ים המפורסם.

---

## 4. עריכת האתר בלי קוד

**חנות מקוונת → ערכות נושא → התאמה אישית** על הת'ים **המפורסם** (`shilov8theme`)
— זה חשוב: עריכה בת'ים לא מפורסם לא תופיע באתר, וכל שינוי כאן נראה לקונים מיד.

### הגדרות כלליות (אייקון גלגל השיניים בתחתית התפריט הימני)

| קטגוריה | מה יש שם |
|---|---|
| **מיתוג** | לוגו, רוחב הלוגו, אייקון האתר |
| **צבעים** | צבע המותג, הכהה הראשי, רקעים, טקסט, קווים, מצבי מלאי |
| **טיפוגרפיה** | גודל כותרות וגוף הטקסט |
| **פריסה וסגנון** | רוחב התוכן, עיגול פינות, אנימציות |
| **כרטיסי מוצר** | יחס תמונה, הצגת מותג/מלאי/מקט, תגיות מבצע, מוצרים בעמוד |
| **עגלה וחיפוש** | סוג העגלה, סכום למשלוח חינם, חיפושים פופולריים |
| **פרטי החנות** | טלפון, וואטסאפ, אימייל, כתובת, שעות, רשתות חברתיות |

**שווה לדעת:** הפרטים שבקטגוריה "פרטי החנות" מוזרמים אוטומטית להדר, לפוטר,
לעמוד צור קשר, לכפתורים הצפים ולנתונים שגוגל קורא. שינוי במקום אחד מתעדכן בכל
האתר.

> **דבר אחד שכדאי לשקול:** תחת **צבעים → "רקע אריחי המחלקות"** שמור כרגע לבן
> (`#FFFFFF`), כי כך זה נשמר בעורך בשלב מסוים. העיצוב תוכנן עם גוון קרם
> (`#EFE9DF`) שמתאחד עם תמונות המחלקות המעוצבות, כך שכל 12 האריחים בדף הבית
> יושבים על אותו משטח. זו החלטה חזותית שלכם — אם רוצים את הקרם, מזינים `#EFE9DF`
> בשדה הזה.

> **אזהרה אחת:** בקטגוריית טיפוגרפיה יש אפשרות "שימוש בגופני שופיפיי". **אל
> תסמנו אותה.** ספריית הגופנים של שופיפיי לא כוללת אותיות עבריות, וסימון
> האפשרות יגרום לכל העברית באתר להיראות בגופן ברירת המחדל של המחשב.

### החלפת הלוגו

הלוגו הנוכחי מוערם — האייקון מעל הכתובית — ולכן בהדר הוא מוצג בגובה מוגבל
(54 פיקסלים בטלפון, 68 במחשב) והכתובית יוצאת קטנה. **אם יש גרסה אופקית של
הלוגו** (האייקון ליד הטקסט ולא מעליו) היא תיראה משמעותית טוב יותר. להחלפה:
הגדרות → מיתוג → לוגו.

### שינוי המחלקות בדף הבית

בעורך, בוחרים את הסקשן **"רשימת קטגוריות"** או **"המחלקות שלנו"**. כל אריח הוא
בלוק — אפשר לגרור לשינוי סדר, ללחוץ על אריח כדי להחליף קטגוריה, להעלות תמונה
מותאמת, או ללחוץ על העין כדי להסתיר.

### תמונת הירו

הירו כרגע מוצג על רקע כחול־כהה מעוצב עם רשת שרטוט — הוא נראה טוב גם בלי תמונה.
להוספת צילום: בעורך בוחרים את **"באנר תמונה"** → **תמונת רקע**. מומלץ 2400×1200
לפחות, ותמונה נפרדת לטלפון. אם התמונה כהה, מקטינים את "כהות שכבת ההצללה".

### עמוד ההזמנה המהירה

הסקשן קיים בתבנית `page.quick-order`. כדי שהעמוד יעבוד צריך **עמוד** בחנות עם
הכתובת `quick-order`: **חנות מקוונת → עמודים → הוספת עמוד**, שם "הזמנה מהירה
לפי מקט", ובקטע "ערכת נושא" בוחרים את התבנית **quick-order**. אותו דבר לעמוד
השאלות הנפוצות — תבנית **faq**.

---

## 5. עדכון הת'ים מהקוד

הת'ים נמצא בתיקיית `theme/` בריפוזיטורי. יש שתי דרכים להעלות שינויים:

### א. חיבור ל-GitHub (מומלץ)

**חנות מקוונת → ערכות נושא → הוספת ערכת נושא → התחברות מ-GitHub**, בוחרים את
הריפוזיטורי ואת הענף. מאותו רגע כל שינוי בקוד מסתנכרן לת'ים אוטומטית. זו הדרך
הנוחה ביותר לתחזוקה שוטפת.

### ב. העלאת קובץ ZIP

```bash
cd theme && zip -r ../shilo-pro-theme.zip . -x "tools/*" "SPEC.md" "DESIGN-SYSTEM.md"
```

ואז **חנות מקוונת → ערכות נושא → הוספת ערכת נושא → העלאת קובץ ZIP**.

> **שתי מלכודות בייבוא ZIP** ששווה להכיר, כי שופיפיי לא מדווח עליהן — הוא פשוט
> משתיק את הקובץ:
> 1. אם ל-`config/settings_schema.json` יש הגדרה עם `"default": ""` ריק, שופיפיי
>    **דוחה את כל הקובץ** ומחליף אותו ב-`[]`. התוצאה: אתר בלי צבעים ובלי גופנים.
> 2. סוגר מסולסל בתוך תג Liquid (למשל `{{ x | append: '{y}' }}`) גורם לדחיית
>    הקובץ כולו.
>
> `python3 theme/tools/validate.py` מזהה את שתי המלכודות לפני העלאה. כדאי להריץ
> אותו תמיד.

---

## 6. פרטי החנות שהוגדרו

| שדה | ערך |
|---|---|
| טלפון | 02-534-3422 |
| אימייל | info@anshilo.com |
| כתובת | קיבוץ קרית ענבים |
| וואטסאפ | https://wa.link/sp55tw |
| שעות | א'-ה' 07:00-17:00 · ו' 07:00-14:00 |
| משלוח חינם מעל | ₪399 |

לשינוי: הגדרות → פרטי החנות.

---

## 7. ביטול האפליקציות בתשלום — 12.75$ לחודש חוזרים אליכם

שתי אפליקציות בתשלום מותקנות היום בחנות, ומה שהן נותנות **מובנה עכשיו בתוך
הת'ים החדש** — בלי עלות חודשית:

### Sense RTL ‏($7.75 לחודש)

מה היא עושה בת'ים הישן, ומה מחליף אותה:

| מה האפליקציה נותנת | התחליף בת'ים החדש |
|---|---|
| הפיכת הת'ים הישן (שנבנה לאנגלית) לעברית מימין-לשמאל | הת'ים החדש נבנה מהיסוד בעברית RTL — אין מה להפוך |
| גופן עברי | גופני Heebo ו-Assistant מוטמעים בתוך הת'ים עצמו |
| תרגום כפתור "Buy now" | כל הטקסטים בת'ים החדש בעברית מלאה |
| **ווידג'ט נגישות** (הכפתור הצף) | ווידג'ט נגישות מובנה: הגדלת טקסט, ניגודיות גבוהה, גווני אפור, היפוך צבעים, הדגשת קישורים, גופן קריא, עצירת אנימציות, סמן גדול, איפוס וקישור להצהרת נגישות. מופעל כברירת מחדל (הגדרות → נגישות) |

> ⚠️ **סדר הפעולות קריטי:** Sense RTL היא זו שמחזיקה את האתר **הנוכחי** בעברית.
> אם תבטלו אותה לפני שהת'ים החדש מפורסם — האתר החי יתהפך לאנגלית משמאל-לימין.
> קודם מפרסמים את הת'ים החדש ובודקים, ורק אחר כך מסירים את האפליקציה.

עוד צעד אחד שעליכם: בהגדרות → נגישות יש שדה "עמוד הצהרת הנגישות" — קשרו אליו
את עמוד ההצהרה שלכם (תקנות הנגישות מחייבות עמוד כזה; יש לכם כבר עמוד נגישות
באתר, ושווה לוודא שהוא מעודכן).

### TA – Product Labels & Badges ‏($5.00 לחודש)

האפליקציה מציגה תוויות על מוצרים. ההגדרות שלה שמורות אצלה בענן ולא ניתן היה
לקרוא אותן מכאן, לכן **לפני הביטול** פִּתחו את האפליקציה ורשמו אילו תוויות
הגדרתם ועל אילו מוצרים.

**חותמות "יבואן רשמי" (מקיטה / מילווקי):** החותמות העגולות שהאפליקציה מציגה
היום מוטמעות עכשיו בת'ים, אבל את קובצי התמונה עצמם צריך להעלות פעם אחת (הם
שמורים אצל האפליקציה ולא בחנות): בעורך הת'ים → הגדרות התבנית → **חותמות
יבואן רשמי** → מעלים את חותמת מקיטה (ארגנטולס) לחותמת 1 ואת חותמת מילווקי
(דלקו) לחותמת 2. שמות המותג ("מקיטה", "מילווקי") כבר מוגדרים. החותמת תופיע
אוטומטית בפינת התמונה של כל מוצר מהמותג — בקטלוג ובעמוד המוצר. אפשר להוסיף
מותג שלישי בחותמת 3.

בת'ים החדש תוויות עובדות דרך תגיות מוצר — בלי אפליקציה:

1. פותחים מוצר בניהול החנות (או כמה מוצרים יחד עם העריכה הקבוצתית)
2. מוסיפים תגית שמתחילה ב-`תווית:` — למשל `תווית:משלוח חינם` או `תווית:מתנה מצורפת`
3. המוצר יציג תווית ענברית עם הטקסט הזה — בכרטיס המוצר בקטלוג ובעמוד המוצר

התוויות האוטומטיות (אחוז הנחה במבצע, "חדש", "אזל מהמלאי") מובנות ממילא ולא
דורשות כלום.

### איך מבטלים בפועל

אחרי שהת'ים החדש פורסם ובדקתם שהכל עובד שבוע-שבועיים:

1. **Settings → Apps and sales channels**
2. לוחצים על האפליקציה → **Uninstall**
3. החיוב החודשי נפסק אוטומטית עם ההסרה

> 💡 באותה הזדמנות: מותקנות גם `whatsapp-button` ו-`essential-announcer`.
> הת'ים החדש כולל כפתורי וואטסאפ/טלפון צפים ופס הודעות מובנים — בדקו אם
> האפליקציות האלה עדיין נחוצות לכם.
