# פרומפטים לסוכן בדפדפן (Claude in Chrome)

שני דברים שדורשים התחברות לחשבונות שלכם ולכן לא יכולים להיעשות מכאן.
כל פרומפט עומד בפני עצמו — להעתיק, להדביק, ולתת לסוכן לעבוד.

**כלל אחד לפני שמתחילים:** אם סוכן מבקש להדביק סיסמה או קוד דו-שלבי — תעשו
את זה אתם, ידנית. אל תכתבו סיסמאות בצ'אט.

---

## פרומפט 1 — Google Play Console: לפתוח את האפליקציה ולהעלות אותה

> אני צריך שתפתח בשבילי אפליקציה חדשה ב-Google Play Console ותכין אותה
> לפרסום. אני מחובר לחשבון. תעבוד שלב-שלב ותעצור לשאול רק אם חסר לך מידע
> שאין ברשימה למטה.
>
> **פרטי האפליקציה**
> - שם: `א.נ. שילו`
> - שפת ברירת מחדל: עברית (he-IL)
> - סוג: אפליקציה (לא משחק)
> - חינמית
> - שם החבילה: `com.anshilo.shop`
>
> **הקובץ להעלאה**
> קובץ ה-AAB נמצא כאן, הורד אותו קודם:
> `https://expo.dev/artifacts/eas/VSG9abdvisw3NvvaeaZCrYoNCvE62AsehODYxiqLS6g.aab`
> גרסה 1.0.1, versionCode 3. תעלה אותו למסלול **Internal testing** קודם, לא
> ל-Production.
>
> **תיאור קצר** (עד 80 תווים)
> `חומרי בניין, כלי עבודה ואספקה טכנית — הקטלוג של א.נ. שילו בטלפון`
>
> **תיאור מלא**
> `א.נ. שילו היא חנות חומרי בניין, כלי עבודה ואספקה טכנית בקרית ענבים, עם יותר משלושים שנה בענף.`
> `באפליקציה תמצאו את כל הקטלוג — קרוב ל-1,900 מוצרים — עם מחירים ומלאי מעודכנים מהחנות עצמה.`
> ``
> `מה יש באפליקציה:`
> `• חיפוש מהיר בכל הקטלוג, וסינון לפי מחיר, מותג וזמינות`
> `• "המומחה של שילו" — יועץ קנייה חכם שמכיר את הקטלוג וממליץ על מה שמתאים לעבודה שלכם`
> `• מועדפים שנשמרים על המכשיר`
> `• עגלה שמסתנכרנת עם החנות ונשמרת בין הפעלות`
> `• התקשרות והודעת וואטסאפ בלחיצה אחת`
> `• איסוף עצמי מהמחסן בקרית ענבים או משלוח עד הבית ולאתר הבנייה`
> ``
> `הצוות שלנו מכיר כל מוצר על המדף. מוזמנים להתקשר 02-534-3422.`
>
> **נכסים גרפיים** — הקבצים בתיקייה `docs/store-assets` בריפוזיטורי, בקש
> ממני להעלות אותם או תן לי לגרור אותם:
> - אייקון 512×512: `icon-512.png`
> - Feature graphic 1024×500: `feature-graphic-1024x500.png`
> - צילומי מסך לטלפון (1080×2400): `01-home.png` עד `05-assistant.png`
>
> **כתובת מדיניות פרטיות**
> `https://anshilo.com/policies/privacy-policy`
>
> **טופס Data Safety — אלה התשובות המדויקות, אל תנחש:**
> - האם האפליקציה אוספת או משתפת נתוני משתמש? **כן**
> - האם הנתונים מוצפנים במעבר? **כן** (הכול HTTPS)
> - האם יש דרך לבקש מחיקת נתונים? **כן**, כתובת:
>   `https://anshilo.com/pages/account-deletion`
> - סוגי נתונים שנאספים:
>   - **Personal info** → Name, Email address, Phone number, Address —
>     נאסף, **מקושר** למשתמש, למטרת **App functionality** (חשבון לקוח
>     והזמנות). לא לשיווק, לא ל-analytics.
>   - **Financial info** → Purchase history — נאסף, **מקושר**, למטרת
>     **App functionality**.
>   - **App activity** → Other user-generated content (טקסט הצ'אט עם העוזר)
>     — נאסף, **לא מקושר** למשתמש, למטרת **App functionality**.
>   - **לא** נאספים: מיקום, אנשי קשר, תמונות, קבצים, בריאות, היסטוריית
>     גלישה, מזהי פרסום.
> - האם הנתונים משמשים למעקב חוצה־אפליקציות או לפרסום? **לא**. אין באפליקציה
>   שום SDK של פרסום, מעקב או אנליטיקה.
>
> **דירוג תוכן** — חנות קמעונאית לחומרי בניין. אין אלימות, אין תוכן מיני,
> אין הימורים, אין אלכוהול או סמים, אין תוכן שנוצר על ידי משתמשים שמוצג
> לאחרים. קהל היעד: מבוגרים, 18+. אין פרסומות.
>
> **סוג האפליקציה לצורך ההצהרות:** אפליקציית קניות/קמעונאות. לא אפליקציית
> בריאות, לא פיננסים, לא ילדים.
>
> כשתסיים — תגיד לי בדיוק מה נשאר לא ממולא ומה Play דורש שאני אעשה בעצמי.

---

## פרומפט 2 — Google Cloud: חשבון שירות ל-`eas submit`

בלי זה `eas submit --platform android` נעצר, וצריך להעלות כל AAB ידנית.
אפשר לדלג על השלב הזה ולהעלות ידנית, אבל אז זה חוזר בכל גרסה.

> אני צריך חשבון שירות של Google שיאפשר ל-EAS להעלות אוטומטית ל-Google Play.
> אני מחובר לשני החשבונות. תעבוד שלב-שלב:
>
> 1. ב-**Google Play Console** → Setup → **API access**. אם עוד לא מקושר
>    פרויקט Google Cloud — תקשר או תיצור אחד.
> 2. ב-**Google Cloud Console** → IAM & Admin → **Service Accounts** →
>    Create service account. שם: `eas-play-publisher`.
> 3. אחרי היצירה → Keys → **Add key** → **Create new key** → **JSON**.
>    הקובץ יורד למחשב. **תגיד לי איפה הוא נשמר ואל תפתח את התוכן שלו בצ'אט.**
> 4. חזרה ל-**Play Console** → API access → תמצא את חשבון השירות החדש →
>    **Grant access**. הרשאות מינימליות שצריך:
>    - Releases: **Release to testing tracks**
>    - Releases: **Release apps to production** (רק אם תרצו הגשה אוטומטית
>      גם ל-production; אחרת לא חובה)
>    - App access: האפליקציה `com.anshilo.shop`
> 5. תאשר את ההזמנה ותוודא שהחשבון מופיע כ-Active.
>
> כשתסיים תגיד לי את הנתיב לקובץ ה-JSON ואת כתובת האימייל של חשבון השירות.

**אחרי שיש קובץ JSON**, מה שנשאר זה שורה אחת ב-`mobile-app/eas.json`, תחת
`submit.production.android`:

```json
"serviceAccountKeyPath": "../play-service-account.json"
```

ולהוסיף את שם הקובץ ל-`.gitignore`. **הקובץ הזה סודי** — הוא מרשה העלאת
גרסאות בשמכם. אל תעלו אותו ל-git ואל תדביקו את תוכנו בצ'אט.

---

## פרומפט 3 — Shopify: טוקן Admin API להלבנת התמונות

זה מה שחסר כדי להריץ `--apply` על 50 התמונות עם הרקע הצבעוני.

> אני צריך טוקן Admin API לחנות שופיפיי שלי, עם שתי הרשאות בלבד. אני מחובר
> לאדמין של `anshilo.com`. תעבוד שלב-שלב:
>
> 1. Settings → **Apps and sales channels** → **Develop apps**.
> 2. אם יש שם אפליקציה בשם **Shilo Mobile App** — תשתמש בה. אם לא —
>    **Create an app**, שם: `Shilo Image Tools`.
> 3. בתוך האפליקציה → **Configuration** → **Admin API integration** →
>    Configure.
> 4. תסמן **בדיוק** את שתי ההרשאות האלה ולא יותר:
>    - `write_products`
>    - `write_files`
> 5. Save.
> 6. **API credentials** → **Install app** → ואז **Reveal token once**.
>
> הטוקן מתחיל ב-`shpat_`. **אל תדביק אותו בצ'אט ואל תשלח לי אותו.** תגיד לי
> רק שהוא מוכן, ואני אעתיק אותו בעצמי מהמסך.

**איפה לשים אותו:** בקובץ `mobile-app/.env`, שורה חדשה:

```
SHOPIFY_ADMIN_TOKEN=shpat_...
```

`.env` כבר ב-`.gitignore`, אז הוא לא יעלה ל-git. **הטוקן הזה סודי** — הוא
מרשה שינוי מוצרים וקבצים בחנות. הוא שונה לגמרי מטוקן ה-Storefront, שהוא
ציבורי מתוקף תפקידו.

אחרי שהוא במקום, הרצה יבשה כדי לוודא שהוא נקרא, ואז ההחלה:

```bash
cd mobile-app
node scripts/whiten-product-images.js                       # יבש, לא נוגע בחנות
node scripts/whiten-product-images.js --apply --vendor "חגית"   # רק הסולמות
node scripts/whiten-product-images.js --apply                  # כל ה-50
```

**מומלץ להתחיל ב-`--vendor "חגית"`** — 32 התמונות שם הן הרקע הנייבי
האמיתי, החד-משמעי. אחרי שרואים שהתוצאה בחנות טובה, להריץ על השאר.
