/**
 * הקונפיג של האפליקציה.
 *
 * app.json נשאר מקור האמת לכל ההגדרות. הקובץ הזה קורא אותו ומאפשר לדרוס
 * **רק** את מזהי החבילה, לפי משתני סביבה.
 *
 * למה זה קיים:
 * מזהה חבילה באפל הוא ייחודי גלובלית ו**בלתי הפיך** - ברגע שחשבון אחד רשם
 * `com.anshilo.shop`, אף חשבון אחר בעולם לא יוכל לרשום אותו, וגם מחיקה לא
 * משחררת אותו באמת. אפליקציה שפורסמה גם לא יכולה להחליף מזהה.
 *
 * בפועל יש כאן שתי זהויות:
 *   - `com.anshilo.shop`       - הזהות האמיתית, בחשבון הארגון של א.נ. שילו בע"מ
 *   - `com.anshilo.shop.test`  - זהות בדיקה, בחשבון אישי, ל-TestFlight פנימי
 *
 * בלי ההפרדה הזאת אפשר בטעות לרשום את המזהה האמיתי בחשבון הלא נכון ולשרוף
 * אותו, או לפרסם את אפליקציית החברה עם מזהה שנגמר ב-`.test` - שתי טעויות
 * שאין מהן חזרה.
 *
 * ברירת המחדל היא תמיד הזהות האמיתית. הבדיקה היא זו שצריכה להצהיר על עצמה.
 */

const { expo } = require('./app.json');

module.exports = () => ({
  ...expo,
  ios: {
    ...expo.ios,
    bundleIdentifier: process.env.SHILO_IOS_BUNDLE_ID ?? expo.ios.bundleIdentifier,
  },
  android: {
    ...expo.android,
    package: process.env.SHILO_ANDROID_PACKAGE ?? expo.android.package,
  },
});
