{%- comment -%}
  Scratch file, safe to delete.

  Used only as a staging target while assets/section-image-banner.css and
  sections/image-banner.liquid were uploaded: the bytes landed here first, the
  checksum was compared against the source, and only the verified copy was
  promoted over the real files. Nothing references this section and it has no
  presets, so it does not appear in the theme editor.
{%- endcomment -%}

{% schema %}
{
  "name": "zz scratch (deletable)"
}
{% endschema %}
